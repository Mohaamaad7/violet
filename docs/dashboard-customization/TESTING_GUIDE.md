# دليل اختبار نظام Zero-Config
# Zero-Config Testing Guide

## 📋 المتطلبات قبل الاختبار

### على السيرفر:
```bash
cd /path/to/violet
git pull
php artisan migrate
php artisan cache:clear
php artisan optimize:clear
```

---

## 🧪 الاختبارات

### اختبار 1: التحقق من ظهور الويدجات تلقائياً

**الهدف:** التأكد من أن كل الـ Widgets تظهر على الـ Dashboard بدون أي تسجيل

**الخطوات:**
1. سجل دخول كـ `super-admin`
2. اذهب للـ Dashboard الرئيسي (`/admin`)
3. تحقق من ظهور الـ Widgets:
   - [ ] TodayRevenueWidget (إيرادات اليوم)
   - [ ] NewOrdersTodayWidget (طلبات جديدة اليوم)
   - [ ] TotalCustomersWidget (إجمالي العملاء)
   - [ ] ProductsInStockWidget (منتجات متاحة)
   - [ ] RecentOrdersWidget (آخر الطلبات)
   - [ ] SalesChartWidget (مخطط المبيعات)
   - [ ] LowStockAlertWidget (تنبيه المخزون المنخفض)
   - [ ] PendingReturnsWidget (المرتجعات المعلقة)
   - [ ] StockValueWidget (قيمة المخزون)
   - [ ] StockMovementsChartWidget (مخطط حركة المخزون)

**النتيجة المتوقعة:** كل الـ Widgets تظهر (لأن super-admin يرى كل شيء)

---

### اختبار 2: صفحة Role Permissions - اكتشاف الـ Widgets

**الهدف:** التأكد من أن صفحة الصلاحيات تكتشف الـ Widgets من الكود

**الخطوات:**
1. اذهب إلى: `System → Role Permissions` (النظام → صلاحيات الأدوار)
2. تحقق من:
   - [ ] القائمة المنسدلة تُظهر كل الأدوار
   - [ ] قسم "Widgets" يُظهر عدد الـ Widgets (مثل: Widgets (10))
   - [ ] كل widget يظهر مع اسمه و group
   - [ ] كل widget لديه زر تبديل (toggle) أخضر (مفعّل)

**النتيجة المتوقعة:** كل الـ Widgets مكتشفة وظاهرة كـ "مفعّلة"

---

### اختبار 3: صفحة Role Permissions - اكتشاف الـ Resources

**الهدف:** التأكد من أن صفحة الصلاحيات تكتشف الـ Resources من الكود

**الخطوات:**
1. في نفس الصفحة، انتقل لقسم "Resources" (الموارد)
2. تحقق من:
   - [ ] جدول الـ Resources يُظهر كل الموارد
   - [ ] كل resource له 4 أعمدة صلاحيات: View, Create, Edit, Delete
   - [ ] كل الصلاحيات مفعّلة (أخضر) بشكل افتراضي
   - [ ] الـ DashboardConfig resources ليست موجودة (system only)

**النتيجة المتوقعة:** كل الـ Resources مكتشفة ولها صلاحيات كاملة

---

### اختبار 4: إخفاء Widget لدور معين

**الهدف:** التأكد من أن الإخفاء يعمل ويتم تخزينه في DB

**الخطوات:**
1. اختر دور غير super-admin (مثل: admin أو editor)
2. في قسم Widgets، اضغط على زر الـ toggle لـ "Today Revenue"
3. تحقق من:
   - [ ] ظهور إشعار "Widget disabled"
   - [ ] الزر تحول للون الأحمر (x-circle)
   - [ ] ظهور badge "Override" بجانب الـ widget

4. افتح قاعدة البيانات وتحقق من جدول `role_widget_defaults`:
```sql
SELECT * FROM role_widget_defaults WHERE widget_class LIKE '%TodayRevenue%';
```
- [ ] يجب أن يوجد record بـ `is_visible = 0`

**النتيجة المتوقعة:** الـ widget مخفي ومسجل في DB

---

### اختبار 5: إعادة تفعيل Widget

**الهدف:** التأكد من أن إعادة التفعيل تحذف الـ override

**الخطوات:**
1. اضغط على نفس زر الـ toggle مرة أخرى
2. تحقق من:
   - [ ] ظهور إشعار "Widget enabled"
   - [ ] الزر تحول للون الأخضر
   - [ ] اختفاء badge "Override"

3. افتح قاعدة البيانات:
```sql
SELECT * FROM role_widget_defaults WHERE widget_class LIKE '%TodayRevenue%';
```
- [ ] يجب أن يكون فارغاً (الـ record تم حذفه)

**النتيجة المتوقعة:** الـ widget مفعّل والـ record محذوف (back to default)

---

### اختبار 6: زر "Enable All" للـ Widgets

**الهدف:** التأكد من أن زر تفعيل الكل يعمل

**الخطوات:**
1. أخفِ عدة widgets (2-3)
2. اضغط "Enable All" (تفعيل الكل)
3. تحقق من:
   - [ ] كل الـ widgets أصبحت مفعّلة (أخضر)
   - [ ] ظهور إشعار "All widgets enabled"

4. تحقق من DB:
```sql
SELECT COUNT(*) FROM role_widget_defaults WHERE role_id = [selected_role_id];
```
- [ ] يجب أن يكون 0

**النتيجة المتوقعة:** كل الـ overrides محذوفة

---

### اختبار 7: تقييد Resource

**الهدف:** التأكد من أن تقييد الصلاحيات يعمل

**الخطوات:**
1. في قسم Resources، ابحث عن "Product"
2. اضغط على زر "Create" لتعطيله
3. تحقق من:
   - [ ] الزر تحول لرمادي
   - [ ] ظهور badge "Override"

4. تحقق من DB:
```sql
SELECT * FROM role_resource_access WHERE resource_class LIKE '%ProductResource%';
```
- [ ] يجب أن يوجد record بـ `can_create = 0`

**النتيجة المتوقعة:** الصلاحية مقيدة ومسجلة

---

### اختبار 8: زر "Grant Full Access"

**الهدف:** التأكد من أن منح الصلاحيات الكاملة يحذف كل القيود

**الخطوات:**
1. قيّد عدة صلاحيات على resources مختلفة
2. اضغط "Grant Full Access" (منح كل الصلاحيات)
3. تحقق من:
   - [ ] كل الصلاحيات مفعّلة
   - [ ] ظهور إشعار "Full access granted"

4. تحقق من DB:
```sql
SELECT COUNT(*) FROM role_resource_access WHERE role_id = [selected_role_id];
```
- [ ] يجب أن يكون 0

**النتيجة المتوقعة:** كل القيود محذوفة

---

### اختبار 9: تسجيل دخول بدور آخر

**الهدف:** التأكد من أن الصلاحيات تُطبق على المستخدم الفعلي

**الخطوات:**
1. أنشئ مستخدم جديد بدور "editor" (أو استخدم موجود)
2. كـ super-admin، أخفِ widget معين عن دور "editor"
3. سجل خروج وسجل دخول كـ "editor"
4. اذهب للـ Dashboard
5. تحقق من:
   - [ ] الـ Widget المخفي لا يظهر

**النتيجة المتوقعة:** الـ widget مخفي للمستخدم ذو الدور المحدد

---

### اختبار 10: إنشاء Widget جديد (Plug & Play)

**الهدف:** التأكد من أن الـ Widget الجديد يظهر تلقائياً

**الخطوات:**
1. أنشئ widget جديد:
```bash
php artisan make:filament-widget TestAutoDiscoverWidget
```

2. أضف كود بسيط للـ widget

3. أعد تحميل صفحة الـ Dashboard
4. تحقق من:
   - [ ] الـ Widget الجديد ظاهر

5. اذهب لصفحة Role Permissions
6. تحقق من:
   - [ ] الـ Widget الجديد موجود في قائمة الـ Widgets

**النتيجة المتوقعة:** لا حاجة لـ `php artisan` أو تسجيل يدوي!

---

### اختبار 11: تغيير اللغة

**الهدف:** التأكد من أن أسماء الـ Widgets تتغير مع اللغة

**الخطوات:**
1. غيّر لغة الـ Admin Panel للإنجليزية
2. تحقق من أسماء الـ Widgets (Today Revenue, etc.)
3. غيّر للعربية
4. تحقق من الأسماء (إيرادات اليوم، etc.)

**النتيجة المتوقعة:** الأسماء تتغير حسب اللغة

---

## ✅ قائمة التحقق النهائية

| الاختبار | الحالة |
|---------|--------|
| 1. ظهور الويدجات تلقائياً | ⬜ |
| 2. اكتشاف الـ Widgets | ⬜ |
| 3. اكتشاف الـ Resources | ⬜ |
| 4. إخفاء Widget | ⬜ |
| 5. إعادة تفعيل Widget | ⬜ |
| 6. Enable All للـ Widgets | ⬜ |
| 7. تقييد Resource | ⬜ |
| 8. Grant Full Access | ⬜ |
| 9. تطبيق الصلاحيات على مستخدم | ⬜ |
| 10. Plug & Play لـ Widget جديد | ⬜ |
| 11. تغيير اللغة | ⬜ |

---

## 🐛 تقرير الأخطاء

إذا وجدت أي مشكلة، سجّلها هنا:

| الاختبار | الخطأ | الخطوات لإعادة الإنتاج |
|---------|-------|----------------------|
| | | |

---

## 📝 ملاحظات

- Super-admin يرى كل شيء دائماً (bypass)
- الـ Cache يُنظف تلقائياً عند التغيير
- DB يخزن الاستثناءات فقط، ليس الحالة الافتراضية
