# 🧪 دليل الاختبار الشامل لنظام المؤثرين

## الفهرس

1. [التحضير للاختبار](#التحضير-للاختبار)
2. [اختبار طلبات التقديم](#اختبار-طلبات-التقديم)
3. [اختبار إدارة المؤثرين](#اختبار-إدارة-المؤثرين)
4. [اختبار العمولات](#اختبار-العمولات)
5. [اختبار طلبات الصرف](#اختبار-طلبات-الصرف)
6. [اختبار Edge Cases](#اختبار-edge-cases)
7. [اختبار الإشعارات](#اختبار-الإشعارات)

---

## التحضير للاختبار

### 1. مسح الكاش

```bash
php artisan cache:clear
php artisan config:clear
php artisan view:clear
```

### 2. التحقق من ظهور Navigation

1. افتح `/admin`
2. تأكد من وجود قائمة **"المؤثرين"** في الـ Sidebar
3. تأكد من وجود 3 روابط:
   - طلبات التقديم
   - المؤثرين
   - طلبات الصرف

### 3. التحقق من الـ Badges

| Resource | Badge يعرض |
|----------|-----------|
| طلبات التقديم | عدد الطلبات pending |
| المؤثرين | إجمالي المؤثرين |
| طلبات الصرف | عدد الطلبات pending |

---

## اختبار طلبات التقديم

### Test 1: عرض قائمة الطلبات

**الخطوات:**
1. اذهب إلى `/admin/influencers/influencer-applications`
2. تحقق من ظهور الجدول

**النتيجة المتوقعة:**
- ✅ الجدول يعرض الأعمدة: الاسم، البريد، الهاتف، المتابعين، الحالة، التاريخ
- ✅ الـ Badge ملون حسب الحالة (أصفر للـ pending)

### Test 2: قبول طلب تقديم

**الخطوات:**
1. اختر طلب بحالة `pending`
2. اضغط زر **"قبول"**
3. أدخل نسبة العمولة (مثال: 10)
4. اضغط **"قبول الطلب"**

**النتيجة المتوقعة:**
- ✅ الطلب يتحول لحالة `approved`
- ✅ يتم إنشاء سجل في جدول `influencers`
- ✅ يتم إنشاء كود خصم للمؤثر في `discount_codes`
- ✅ إشعار نجاح يظهر

**التحقق في قاعدة البيانات:**
```sql
-- التحقق من المؤثر
SELECT * FROM influencers WHERE user_id = [USER_ID];

-- التحقق من الكود
SELECT * FROM discount_codes WHERE influencer_id = [INFLUENCER_ID];
```

### Test 3: رفض طلب تقديم

**الخطوات:**
1. اختر طلب بحالة `pending`
2. اضغط زر **"رفض"**
3. أدخل سبب الرفض
4. اضغط **"رفض الطلب"**

**النتيجة المتوقعة:**
- ✅ الطلب يتحول لحالة `rejected`
- ✅ يتم حفظ سبب الرفض
- ✅ إشعار نجاح يظهر

---

## اختبار إدارة المؤثرين

### Test 4: عرض قائمة المؤثرين

**الخطوات:**
1. اذهب إلى `/admin/influencers/influencers`

**النتيجة المتوقعة:**
- ✅ يظهر المؤثرين الـ active
- ✅ تظهر الإحصائيات (المبيعات، الرصيد)

### Test 5: تعليق مؤثر

**الخطوات:**
1. اختر مؤثر active
2. اضغط زر **"تعليق"**
3. أكد العملية

**النتيجة المتوقعة:**
- ✅ الحالة تتحول إلى `suspended`
- ✅ أكواد الخصم للمؤثر تصبح غير فعالة (Edge Case Test)

### Test 6: تفعيل مؤثر

**الخطوات:**
1. اختر مؤثر suspended
2. اضغط زر **"تفعيل"**

**النتيجة المتوقعة:**
- ✅ الحالة تتحول إلى `active`

---

## اختبار العمولات

### Test 7: تسجيل عمولة تلقائياً

**الخطوات:**
1. تأكد من وجود مؤثر active مع كود خصم
2. من الـ Frontend: أنشئ طلب واستخدم كود المؤثر
3. أكد الدفع (إما عبر COD أو online payment)
4. من الـ Admin: غير payment_status إلى `paid`

**النتيجة المتوقعة:**
- ✅ يتم إنشاء سجل في `influencer_commissions`
- ✅ الـ status = `pending`
- ✅ يتم تحديث `total_sales` و `balance` للمؤثر

**التحقق:**
```sql
SELECT * FROM influencer_commissions WHERE order_id = [ORDER_ID];
SELECT total_sales, balance FROM influencers WHERE id = [INFLUENCER_ID];
```

### Test 8: إلغاء عمولة عند إلغاء الطلب

**الخطوات:**
1. من الـ Admin: ألغِ الطلب الذي سجلت له عمولة

**النتيجة المتوقعة:**
- ✅ الـ commission.status يتحول إلى `cancelled`
- ✅ يتم عكس الإحصائيات من المؤثر

---

## اختبار طلبات الصرف

### Test 9: إنشاء طلب صرف

**الخطوات:**
1. اذهب إلى `/admin/influencers/commission-payouts`
2. اضغط **"إنشاء"**
3. اختر المؤثر
4. أدخل المبلغ
5. اختر طريقة الدفع
6. أدخل تفاصيل الحساب
7. احفظ

**النتيجة المتوقعة:**
- ✅ يتم إنشاء الطلب بحالة `pending`

### Test 10: الموافقة على طلب صرف

**الخطوات:**
1. اختر طلب pending
2. اضغط **"موافقة"**

**النتيجة المتوقعة:**
- ✅ الحالة تتحول إلى `approved`
- ✅ يتم حفظ approved_by و approved_at

### Test 11: تنفيذ الصرف (Mark as Paid)

**الخطوات:**
1. اختر طلب approved
2. اضغط **"تم الدفع"**
3. أدخل رقم المرجع (مثال: TRX123456)
4. أكد

**النتيجة المتوقعة:**
- ✅ الحالة تتحول إلى `paid`
- ✅ يتم حفظ transaction_reference, paid_by, paid_at
- ✅ balance للمؤثر يقل بمقدار المبلغ
- ✅ العمولات المرتبطة تتحول لـ `paid`

**التحقق:**
```sql
SELECT balance FROM influencers WHERE id = [INFLUENCER_ID];
SELECT status, payout_id FROM influencer_commissions 
WHERE influencer_id = [INFLUENCER_ID];
```

### Test 12: رفض طلب صرف

**الخطوات:**
1. اختر طلب pending
2. اضغط **"رفض"**
3. أدخل سبب الرفض
4. أكد

**النتيجة المتوقعة:**
- ✅ الحالة تتحول إلى `rejected`
- ✅ يتم حفظ rejection_reason

---

## اختبار Edge Cases

### Test 13: استخدام كود مؤثر موقوف

**الخطوات:**
1. من الـ Admin: علق المؤثر (suspended)
2. من الـ Frontend: حاول استخدام كود الخصم

**النتيجة المتوقعة:**
- ✅ رسالة خطأ: "هذا الكود غير متاح حالياً"
- ✅ الكود لا يُطبق

### Test 14: قبول طلب مرتين

**الخطوات:**
1. حاول الضغط على "قبول" لطلب approved

**النتيجة المتوقعة:**
- ✅ الزر لا يظهر (visible: fn => status === pending)

### Test 15: تسجيل عمولة لكود عادي (ليس مؤثر)

**الخطوات:**
1. استخدم كود خصم عادي (بدون influencer_id)
2. أكد الدفع

**النتيجة المتوقعة:**
- ✅ لا يتم إنشاء سجل في `influencer_commissions`
- ✅ الطلب يُنفذ بشكل عادي

### Test 16: مؤثر غير نشط

**الخطوات:**
1. في DB: غير status لـ 'inactive' (ليس suspended)
2. استخدم الكود

**النتيجة المتوقعة:**
- ✅ رسالة خطأ: "كود الخصم غير صالح"

---

## اختبار الإشعارات

### Test 17: إشعار قبول الطلب

**الخطوات:**
1. اقبل طلب تقديم
2. تحقق من جدول `notifications`

**التحقق:**
```sql
SELECT * FROM notifications 
WHERE type LIKE '%ApplicationApproved%'
ORDER BY created_at DESC LIMIT 1;
```

### Test 18: إشعار العمولة

**الخطوات:**
1. سجل عمولة لمؤثر
2. تحقق من جدول `notifications`

**التحقق:**
```sql
SELECT * FROM notifications 
WHERE type LIKE '%CommissionEarned%'
ORDER BY created_at DESC LIMIT 1;
```

---

## Checklist سريع

| # | الاختبار | الحالة |
|---|---------|--------|
| 1 | Navigation ظاهر | ☐ |
| 2 | عرض طلبات التقديم | ☐ |
| 3 | قبول طلب | ☐ |
| 4 | رفض طلب | ☐ |
| 5 | عرض المؤثرين | ☐ |
| 6 | تعليق مؤثر | ☐ |
| 7 | تفعيل مؤثر | ☐ |
| 8 | تسجيل عمولة تلقائي | ☐ |
| 9 | إلغاء عمولة | ☐ |
| 10 | إنشاء طلب صرف | ☐ |
| 11 | موافقة على صرف | ☐ |
| 12 | تنفيذ صرف | ☐ |
| 13 | رفض صرف | ☐ |
| 14 | كود مؤثر موقوف | ☐ |
| 15 | إشعار قبول | ☐ |
| 16 | إشعار عمولة | ☐ |
