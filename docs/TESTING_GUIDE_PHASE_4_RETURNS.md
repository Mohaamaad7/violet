# 📋 دليل اختبار نظام المرتجعات (Phase 4) - للمستخدم النهائي

**التاريخ:** 13 ديسمبر 2025  
**الإصدار:** 1.0  
**الحالة:** ✅ جاهز للاختبار

---

## 🎯 الهدف من هذا الدليل

هذا الدليل يساعدك على اختبار نظام المرتجعات بالكامل والتأكد من أن جميع الميزات تعمل بشكل صحيح بدون أخطاء.

---

## 📦 المتطلبات الأولية

قبل البدء في الاختبار، تأكد من:

1. ✅ قاعدة البيانات تحتوي على بيانات تجريبية (Products, Customers, Orders)
2. ✅ تم تشغيل جميع الـ Migrations بنجاح
3. ✅ تم تشغيل الـ Seeders (خصوصًا `ReturnPolicySettingsSeeder`)
4. ✅ لديك حساب أدمن للدخول إلى لوحة التحكم

### أوامر التجهيز السريعة:

```powershell
# 1. تحديث قاعدة البيانات
php artisan migrate:fresh --seed

# 2. تشغيل seeder إعدادات المرتجعات
php artisan db:seed --class=ReturnPolicySettingsSeeder

# 3. إنشاء بيانات تجريبية (إذا لم تكن موجودة)
php artisan tinker
# ثم في Tinker:
# Customer::factory()->count(5)->create();
# Product::factory()->count(20)->create();
# Order::factory()->count(10)->create();
```

---

## 🧪 سيناريوهات الاختبار

### 1️⃣ اختبار إنشاء طلب مرتجع (Return Request Creation)

#### أ. مرتجع بعد التوصيل (Return After Delivery)

**الخطوات:**
1. افتح لوحة التحكم → Orders
2. اختر طلب بحالة **"Delivered"** (تم التوصيل)
3. انقر على "View" → ابحث عن زر "Create Return"
4. املأ النموذج:
   - **النوع:** Return after delivery
   - **السبب:** مثال: "المنتج لا يطابق الوصف"
   - **العناصر:** اختر عنصر واحد على الأقل
   - **ملاحظات العميل:** أي نص توضيحي
5. اضغط "Submit"

**النتيجة المتوقعة:**
- ✅ ظهور رسالة نجاح
- ✅ إنشاء سجل جديد في جدول Returns بحالة **"Pending"**
- ✅ توليد رقم مرتجع فريد (مثل: RET-2025-001)

---

#### ب. رفض الاستلام (Rejection)

> ⚠️ **ملاحظة مهمة:** عند رفض الاستلام، يتم إرجاع **جميع أصناف الطلب** تلقائياً. لا يوجد رفض جزئي.

**الخطوات:**
1. افتح لوحة التحكم → Orders
2. اختر طلب بحالة **"Shipped"** (قيد الشحن)
3. انقر على "View" → ابحث عن زر **"رفض الاستلام"**
4. ستظهر رسالة تحذيرية: "سيتم إرجاع جميع أصناف الطلب تلقائياً"
5. املأ النموذج:
   - **السبب:** مثال: "تأخير الشحن" أو "العميل لا يرد" أو "ألغى الطلب"
   - ⚠️ **لا يوجد اختيار أصناف** - كل الأصناف تُرجع تلقائياً
6. اضغط "Submit"

**النتيجة المتوقعة:**
- ✅ إذا كان الإعداد `auto_approve_rejections` مفعّل:
  - الحالة تصبح **"Approved"** مباشرة
- ✅ إذا كان معطّل:
  - الحالة تكون **"Pending"**
- ✅ جميع أصناف الطلب مضافة في return_items

**❌ حالة خطأ (يجب أن تفشل):**
- محاولة إنشاء رفض على طلب بحالة **"Delivered"** → يجب ظهور خطأ: "Order cannot be rejected in current status"

---

### 2️⃣ اختبار الموافقة على طلب مرتجع (Approve Return)

**الخطوات:**
1. افتح لوحة التحكم → Returns
2. اختر طلب مرتجع بحالة **"Pending"**
3. انقر على زر "Approve"
4. أدخل ملاحظات الأدمن (اختياري)
5. اضغط "Confirm"

**النتيجة المتوقعة:**
- ✅ تغيير الحالة إلى **"Approved"**
- ✅ تسجيل `approved_by` (معرّف الأدمن الذي وافق)
- ✅ تسجيل `approved_at` (وقت الموافقة)
- ✅ ظهور الطلب في قائمة المرتجعات الموافق عليها

---

### 3️⃣ اختبار رفض طلب مرتجع (Reject Return)

**الخطوات:**
1. افتح لوحة التحكم → Returns
2. اختر طلب مرتجع بحالة **"Pending"**
3. انقر على زر "Reject"
4. أدخل سبب الرفض (مطلوب): مثال: "خارج نطاق السياسة"
5. اضغط "Confirm"

**النتيجة المتوقعة:**
- ✅ تغيير الحالة إلى **"Rejected"**
- ✅ تسجيل `rejected_by` (معرّف الأدمن)
- ✅ تسجيل `rejected_at` (وقت الرفض)
- ✅ حفظ سبب الرفض في `admin_notes`

---

### 4️⃣ اختبار معالجة المرتجع وإرجاع المخزون (Process Return & Restock)

**الخطوات:**
1. افتح لوحة التحكم → Returns
2. اختر طلب مرتجع بحالة **"Approved"**
3. انقر على زر "Process Return"
4. لكل عنصر في المرتجع:
   - **الحالة:** اختر (Good / Opened / Damaged)
   - **الكمية المستلمة:** أدخل الكمية الفعلية (قد تكون أقل من الكمية المطلوبة)
   - **ملاحظات:** أي ملاحظات إضافية
5. اضغط "Submit"

**النتيجة المتوقعة:**
- ✅ تغيير حالة المرتجع إلى **"Completed"**
- ✅ تسجيل `processed_by` و `processed_at`
- ✅ **إرجاع المخزون:**
  - العناصر بحالة **"Good"** أو **"Opened"** → تعود للمخزون
  - العناصر بحالة **"Damaged"** → لا تعود للمخزون
- ✅ حساب `refund_amount` (مبلغ الاسترداد)
- ✅ تحديث `refund_status` إلى "Pending"

**مثال عملي:**
- طلب مرتجع يحتوي على 5 وحدات من المنتج X
- الأدمن يستلم 3 وحدات فقط (good)
- النظام يضيف 3 وحدات فقط للمخزون
- يحسب الاسترداد على أساس 3 وحدات

---

### 5️⃣ اختبار منع المعالجة المكررة (Prevent Double Processing)

**الخطوات:**
1. عالج مرتجع (كما في الخطوة السابقة)
2. حاول معالجة نفس المرتجع مرة أخرى

**النتيجة المتوقعة:**
- ❌ يجب ظهور خطأ: "Return can only be processed when status is approved"

---

### 6️⃣ اختبار فترة الإرجاع (Return Window Validation)

**الخطوات:**
1. افتح Settings → Returns
2. تحقق من قيمة `return_window_days` (مثال: 14 يوم)
3. حاول إنشاء مرتجع على طلب تم توصيله قبل أكثر من 14 يوم

**النتيجة المتوقعة:**
- ❌ يجب ظهور خطأ: "Return window has expired (allowed: 14 days)"

---

### 7️⃣ اختبار منع التكرار (Prevent Duplicate Returns)

**الخطوات:**
1. أنشئ طلب مرتجع على طلب معين
2. حاول إنشاء طلب مرتجع آخر على نفس الطلب (قبل معالجة الأول)

**النتيجة المتوقعة:**
- ❌ يجب ظهور خطأ: "Order already has a pending or approved return request"

---

### 8️⃣ اختبار الموافقة التلقائية على الرفض (Auto-Approve Rejections)

**الخطوات:**
1. افتح Settings → Returns
2. فعّل الإعداد `auto_approve_rejections`
3. أنشئ طلب رفض جديد (Rejection)

**النتيجة المتوقعة:**
- ✅ الحالة تصبح **"Approved"** مباشرة بدون انتظار

**الخطوات (اختبار معاكس):**
1. عطّل الإعداد `auto_approve_rejections`
2. أنشئ طلب رفض جديد

**النتيجة المتوقعة:**
- ✅ الحالة تكون **"Pending"** وتحتاج موافقة يدوية

---

### 9️⃣ اختبار المرتجعات الجزئية (Partial Returns)

**الخطوات:**
1. افتح Settings → Returns
2. تأكد من أن `allow_partial_returns` مفعّل
3. أنشئ طلب مرتجع يحتوي على عنصر بكمية 5 وحدات
4. عند المعالجة، أدخل `received_quantity = 3` فقط

**النتيجة المتوقعة:**
- ✅ النظام يقبل المعالجة
- ✅ يعيد 3 وحدات فقط للمخزون
- ✅ يحسب الاسترداد على أساس 3 وحدات

**الخطوات (اختبار معاكس):**
1. عطّل الإعداد `allow_partial_returns`
2. حاول إنشاء مرتجع جزئي

**النتيجة المتوقعة:**
- ❌ يجب أن يُرفض أو يُمنع

---

## 🔍 فحص قاعدة البيانات

بعد كل عملية، افحص الجدول `returns` في قاعدة البيانات:

```sql
SELECT 
    id,
    return_number,
    type,
    status,
    approved_by,
    approved_at,
    rejected_by,
    rejected_at,
    processed_by,
    processed_at,
    refund_amount,
    refund_status
FROM returns
ORDER BY created_at DESC
LIMIT 10;
```

**تحقق من:**
- ✅ جميع الحقول تُملأ بشكل صحيح
- ✅ التوقيتات (`approved_at`, `rejected_at`, `processed_at`) مسجلة
- ✅ معرّفات المستخدمين (`approved_by`, `rejected_by`, `processed_by`) صحيحة

---

## 🧪 اختبار تلقائي (Automated Tests)

لتشغيل جميع الاختبارات التلقائية:

```powershell
# اختبارات Feature (20 اختبار)
php artisan test --filter="ReturnPolicyTest|ReturnServiceTest" tests/Feature/

# اختبارات Unit (10 اختبار)
php artisan test tests/Unit/Services/ReturnServiceTest.php

# اختبارات Resource (Filament) (13 اختبار)
php artisan test tests/Feature/ReturnResourceTest.php

# جميع اختبارات المرتجعات
php artisan test --filter=Return
```

**النتيجة المتوقعة:**
- ✅ جميع الاختبارات تمر بنجاح (100%)
- ❌ إذا فشل أي اختبار، راجع رسالة الخطأ وتأكد من الإعدادات

---

## 📊 تقرير الأداء والإحصائيات

### اختبار صفحة الإحصائيات:

**الخطوات:**
1. افتح لوحة التحكم → Returns → Statistics
2. تحقق من:
   - ✅ إجمالي المرتجعات
   - ✅ توزيع حسب الحالة (Pending, Approved, Rejected, Completed)
   - ✅ توزيع حسب النوع (Rejection, Return after delivery)
   - ✅ نسبة المرتجعات (Return Rate)
   - ✅ إجمالي مبالغ الاسترداد
   - ✅ متوسط مبلغ الاسترداد

---

## 🔐 اختبار الصلاحيات (Permissions)

### اختبار صلاحيات الأدمن:

**الخطوات:**
1. سجّل دخول كأدمن عادي (ليس Super Admin)
2. حاول الوصول إلى صفحة Returns

**النتيجة المتوقعة:**
- ✅ إذا كان لديه صلاحية `view_order::return` → يستطيع الدخول
- ❌ إذا لم يكن لديه الصلاحية → يُمنع الوصول

---

## 🚨 حالات الخطأ التي يجب اختبارها

| الحالة | الخطأ المتوقع |
|--------|---------------|
| إنشاء رفض على طلب Delivered | "Order cannot be rejected in current status" |
| إرجاع بعد التوصيل على طلب Pending | "Order must be delivered before creating return" |
| إنشاء مرتجع بعد انتهاء فترة الإرجاع | "Return window has expired" |
| محاولة معالجة مرتجع غير موافق عليه | "Return can only be processed when status is approved" |
| معالجة نفس المرتجع مرتين | "Return has already been processed" |
| إنشاء مرتجع مكرر على نفس الطلب | "Order already has a pending or approved return request" |

---

## ✅ قائمة التحقق النهائية (Final Checklist)

- [ ] جميع الاختبارات التلقائية تمر (43 اختبار)
- [ ] إنشاء مرتجع بعد التوصيل يعمل
- [ ] إنشاء رفض على طلب Pending يعمل
- [ ] الموافقة على المرتجع تسجل البيانات صحيحة
- [ ] رفض المرتجع يسجل البيانات صحيحة
- [ ] معالجة المرتجع تُرجع المخزون بشكل صحيح
- [ ] العناصر التالفة لا تعود للمخزون
- [ ] المرتجعات الجزئية تعمل (إذا مفعّلة)
- [ ] منع المعالجة المكررة يعمل
- [ ] فترة الإرجاع تُطبّق بشكل صحيح
- [ ] الموافقة التلقائية على الرفض تعمل (إذا مفعّلة)
- [ ] الإحصائيات تظهر أرقام صحيحة
- [ ] قاعدة البيانات تحتوي على جميع الأعمدة المطلوبة
- [ ] الصلاحيات تُطبّق بشكل صحيح

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشكلة أثناء الاختبار:

1. راجع رسالة الخطأ في `storage/logs/laravel.log`
2. تحقق من أن جميع الـ Migrations تم تشغيلها
3. تأكد من أن الـ Seeders تم تشغيلها بشكل صحيح
4. راجع التوثيق الكامل في `docs/PHASE_4_RETURNS_MANAGEMENT_COMPLETE.md`

---

**انتهى دليل الاختبار ✅**

**التاريخ:** 13 ديسمبر 2025  
**حالة النظام:** جاهز للإنتاج 🚀
